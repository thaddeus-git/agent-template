# External references

Pinned third-party API behavior the code relies on. Each file
documents one external system + one topic, with a fetched date so
drift is detectable.

Use [`0000-template.md`](0000-template.md) as the starting point.

## Index

<!-- Add a row when you pin a new external API. -->

| Reference | What it pins |
|---|---|
| _example: example-api-rate-limits.md_ | _Rate limits and 429 retry semantics for Example API._ |
